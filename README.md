# Description 
a small compiler to extract parse tree for codebase , using Javascript
# Example
if(x==5) {x=x+5} elseif(x==3) {x=x+3} elseif(x==1) {x=x+1} else{x=x+6}
output will be the abstract tree for this code
